# NerdsWhoFish

Shared GitHub configuration for the Dusk project.

`.github/workflows/plugin-release.yml` is the release workflow every `dusk-plugin-*` repository calls, so plugins are packaged identically and Dusk can install any of them the same way.
